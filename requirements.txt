requests>=2.32
beautifulsoup4>=4.13
lxml>=5.2
trafilatura>=2.0
pyyaml>=6.0
# google-genai>=2.8,<3.0   # LLM_PROVIDER=gemini 로 쓸 때만 주석 해제
# 같은 사건 거르기(news/core/similar.py). 수집 기계에만 깐다 — torch 가 커서 CI 에는
# 넣지 않는다. 없으면 그 단계만 건너뛴다.
#   sentence-transformers>=5   (BAAI/bge-m3)
