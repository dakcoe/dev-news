# EXEC_PLAN: add-article-tags

- **타입**: feat
- **프로젝트**: project/dev-news
- **상태**: IN_PROGRESS
- **생성일**: 2026-08-09T03:07:23.643Z

## 목표

SPEC 1B(태그 도입)의 1차 구현. 누적 179건 코퍼스 분석으로 **닫힌 태그 어휘(범용 카테고리 ~19개)** 를 확정하고,
규칙 기반(키워드 매칭) 태거로 기사당 평균 3~4개 태그를 부여한다. 기존 기사 소급 태깅 + 신규 파이프라인 태깅 + 태그 칩 UI(1차 필터)까지.

## 접근법

- **닫힌 어휘 강제** (SPEC "LLM 자유 태그 생성 금지"): 어휘는 코드에 상수로 고정.
- **규칙 기반 태거** 채택 (LLM 배치 태깅 대신): 제목·ko_title·설명·요약에 키워드 패턴 매칭.
  - 이유: ① Groq 예산 0, ② 결정적·테스트 가능, ③ 소급 태깅을 언제든 재실행 가능(어휘 개정 시 전체 재태깅이 공짜).
  - 요약 실패·한자 혼입 기사도 영문 원제로 매칭되므로 데이터 품질 이슈에 강함.
- **암시 규칙**: llm/ai-agent/ai-coding 등 세부 AI 태그 → 상위 `ai` 자동 부여. `source==github` → `open-source`.
  평균 3~4개는 "도메인 태그 + 상위 태그 + 유형 태그(release/showcase)"의 직교 축 조합으로 달성.
- 소급 태깅은 이번 달 샤드(2026-08.json)만 수정 — "지난 달 샤드 불변" 규칙 준수 (현재 샤드는 이번 달뿐).

## 단계별 계획

1. 코퍼스 분석(179건 + candidates 634건)으로 어휘 초안 → 태거 구현(`news/core/tags.py`) → 실측 분포로 어휘 보정 (평균 3~4개, 무태그 최소화)
2. pytest 테스트 작성: 어휘 무결성, 대표 기사 태깅 정확성, 실코퍼스 평균 3~4개·커버리지 검증
3. 파이프라인 통합: `build.py`에서 게시 직전 태깅, `archive.write_search_index`에 태그 포함(SPEC 2.2)
4. 소급 태깅 스크립트(`scripts/retag.py` — 프로젝트 내)로 기존 179건에 tags 필드 부여, docs/data 동기화
5. UI: 행에 태그 칩 표시 + 태그 필터 칩(1차 필터, localStorage 저장 — SPEC 3.3), 소스 칩은 유지
6. `--demo` 렌더 확인 → data 기준 재렌더 → verify-task.js → 커밋

## 완료 기준

- [ ] pytest 전체 통과 (기존 테스트 포함)
- [ ] 실코퍼스 기준 기사당 평균 태그 3.0~4.5개, 태그 0개 기사 5% 미만
- [ ] data/articles/2026-08.json 전 기사에 `tags` 필드 존재
- [ ] search-index.json에 태그 포함, docs/index.html에 태그 칩 렌더·필터 동작
- [ ] `python build.py --demo` 정상 동작 (기존 기능 회귀 없음)
