# Astra 읽기 전용 검토

검토 기준: `9f397d9` (`origin/main`, 2026-09-20 확인). 코드·테스트·수집 데이터는 수정하지 않았다. 기존 Python 테스트는 `PYTHONDONTWRITEBYTECODE=1 venv/bin/python -m pytest tests/ -q -p no:cacheprovider`로 **618 passed in 1.12s**, Worker의 `node worker/test.mjs`는 **통과**했다. 아래 실행 재현은 기존 함수를 메모리 mock으로 호출한 결과이며, 테스트 파일을 추가하지 않았다.

`scripts/graph.py --gaps`의 코드 연결 누락 13개와 테스트 연결 누락 4개는 전부 대조했다. 알려진 빚은 재보고하지 않았다. 현재 시각·최근성 검사와 고정 날짜 사용을 확인한 범위에서는 추가 시한부 테스트를 발견하지 못했다. 알려진 SEO 사례는 `tests/test_seo.py:244`에서 상대 날짜를 사용한다. 실제 수집·외부 API 호출·재렌더·운영 배포는 실행하지 않았다.

## 동작이 틀림

위치  news/core/archive.py:95, news/core/seen.py:70, build.py:356  
무엇  재등장 기간이 지나 다시 선별한 GitHub 저장소가 아카이브의 URL 중복 검사에서 제거된다.  
근거  `seen.filter_unseen()`은 기간이 지난 항목에 `resurfaced`를 붙여 통과시키지만 `archive.append()`는 `if ... not in known`으로 기존 URL을 제외한다; 30일 규칙의 메모리 재현 결과는 `resurface picked = 1, new shard rows = 0`이다.  
영향  재요약한 기사가 새 회차에 실리지 않는데도 seen 시각과 게시 건수가 갱신되어, 다시 트렌딩 기능이 동작하지 않고 요약 호출도 낭비된다.  
제안  재등장 기사의 새 회차 저장을 일반 URL 중복 방지와 구분하고, 실제 저장된 항목을 기준으로 seen과 게시 건수를 갱신한다.

위치  news/core/enrich.py:86, docs/data/articles/2026-08.json:13986  
무엇  GitHub 이슈·커밋·릴리스 URL도 해당 페이지 대신 저장소 README를 수집한다.  
근거  `/anthropics/claude-code/issues/6235`의 메모리 재현에서 `_github_readme('anthropics', 'claude-code')`만 호출되고 이슈 요청은 0회였다; 저장된 원제는 `Feature Request: Support AGENTS.md`지만 13996~13997줄의 번역 제목·요약은 Claude Code 제품 소개와 설치 방법이다.  
영향  이슈나 릴리스 자체의 내용을 수집하지 않아 다른 문서의 정보를 기사 내용으로 게시한다.  
제안  README 우회를 저장소 루트 URL에 한정하고, 이슈·커밋·릴리스 주소는 해당 페이지의 본문을 수집한다.

위치  news/template.html:984, news/template.html:1252, worker/src/index.js:112  
무엇  로그인한 사용자가 읽음을 해제해도 서버에는 해제가 전달되지 않는다.  
근거  `toggleRead()`는 `read.delete(u)`를 실행하지만 `syncQueueRead()`는 새 읽음의 `rd`만 추가하고 Worker에도 해제 연산이 없다; 기존 읽음 1건을 해제한 실행 결과는 `localRead: false, serverKnownRead: true, pending: []`였다.  
영향  다른 기기에서는 계속 읽음으로 보이고, 다음 `syncPull()`이 서버 값을 적용하면 같은 기기의 해제 표시도 다시 읽음으로 돌아온다.  
제안  읽음 해제를 나타내는 연산을 클라이언트와 Worker 양쪽에 정의하고 서버 기록에서도 제거한다.

위치  news/template.html:1011, news/template.html:1020  
무엇  동기화 POST가 401을 반환하면 미전송 변경을 성공 처리해 큐에서 버린다.  
근거  `splice(0, 500)`으로 꺼낸 뒤 401에서 `syncUser = null; ... ok = true`로 설정한다; 보관 추가 1건과 401 응답을 넣은 실행 결과는 `pending: 0, user: null`이었다.  
영향  세션이 만료된 순간의 보관 추가·해제와 읽음 변경을 재로그인 후 재전송할 수 없고, 이미 동기화한 브라우저에서는 서버 정본을 다시 받아 로컬 변경도 잃는다.  
제안  401에서는 변경을 큐에 돌려놓고 전송을 중단하며, 재로그인한 계정을 확인한 뒤 재전송한다.

위치  news/template.html:1075, news/template.html:1249  
무엇  미전송 변경이 남아 있어도 서버 조회가 성공하면 로컬 보관함과 읽음 상태를 서버 값으로 덮어쓴다.  
근거  초기화는 `await syncFlush(); syncPull()` 순서지만 전송 실패 여부를 확인하지 않는다; 이미 동기화한 브라우저에서 보관 추가 POST=503, 빈 보관함 GET=200을 재현하면 `pending: 1, visibleSaved: 0, storedSaved: '[]'`가 된다.  
영향  아직 서버에 도착하지 않은 보관 항목이 화면과 로컬 저장소에서 사라지고, 미전송 해제는 반대로 다시 나타난다; 큐가 남아 있어 영구 유실과는 구분되지만 사용자에게 표시되는 상태가 틀리다.  
제안  서버 조회 결과에 미전송 연산을 적용한 상태를 표시하거나, 미전송 변경이 남은 동안 정본 교체를 보류한다.

위치  news/template.html:2032, news/template.html:2040  
무엇  검색·무한 스크롤의 부분 갱신마다 푸터가 하나씩 추가된다.  
근거  `layoutHTML()`은 `.layout`을 닫은 뒤 형제 요소인 `FOOT_HTML`까지 반환하지만 `renderList()`는 `.layout`만 `outerHTML`로 교체한다; 반환 문자열과 교체 범위를 재현하면 초기 푸터 1개가 갱신 3회 뒤 4개가 된다.  
영향  검색 입력이나 다음 기사 묶음 로딩을 반복할수록 소개·개인정보 처리·저작권 링크가 목록 아래 중복된다.  
제안  부분 갱신 함수가 반환하고 교체하는 범위를 일치시키고 푸터는 그 밖에서 한 번 그린다; 단일 HTML 산출은 유지할 수 있으며, 이 오류 때문에 2,445줄 템플릿 전체를 분리할 필요는 없다.

## 위험

위치  news/core/archive.py:41, news/core/archive.py:103  
무엇  기존 월별 샤드 읽기 실패를 빈 목록으로 처리한 뒤 같은 파일을 새 기사만으로 덮어쓸 수 있다.  
근거  `_load_json()`은 예외에 `return []`를 하고 `append()`는 `stamped + _load_json(...)`을 즉시 저장한다; 손상 JSON 읽기와 저장을 메모리 mock한 결과 예외 없이 `_save_json('/mock/2026-09.json', [새 기사])`가 호출됐다.  
영향  샤드 파싱 오류나 읽기 실패가 발생하면 그달 기존 기사들이 최신 정본에서 사라지고 파생 페이지·검색 색인에서도 누락된다; 현재 저장 데이터가 손상됐다는 발견은 아니다.  
제안  파일 부재와 기존 파일 읽기 실패를 구분하고, 후자에서는 저장·발행을 중단해 원본을 보존한다.

위치  scripts/publish.sh:54, scripts/publish.sh:58  
무엇  원격 데이터 합집합 생성에 실패해도 충돌 선택을 강제하는 rebase와 push가 계속된다.  
근거  셸 옵션은 `set -uo pipefail`이며 `"$PY" scripts/merge_remote_data.py origin/main`의 종료 상태 검사가 없다; 곧바로 `git pull --rebase -X theirs`로 진행하고, `scripts/merge_remote_data.py:4`는 합집합 없이 이 rebase를 하면 원격 기사·seen 기록을 잃는다고 설명한다.  
영향  병합 스크립트가 실패한 회차에서 원격에만 있던 기록을 잃은 채 발행할 수 있다; 실제 자동화나 git 병합을 실행해 사고를 유발하지는 않았다.  
제안  합집합 생성 실패 시 알림을 남기고 즉시 종료하며, 초기 pull·commit·fetch·최종 rebase도 성공 여부를 확인한 뒤 다음 단계로 진행한다.

위치  news/template.html:1463, news/template.html:2069  
무엇  월별 검색 색인 일부의 로딩 실패가 정상 검색 상태로 굳어져 사용자에게 드러나지 않는다.  
근거  월별 오류는 `r.ok ? r.json() : []`와 `.catch(()=>[])`로 숨기며 하나라도 결과가 있으면 `INDEX=all`이 된다; 8월=503·9월=정상 응답을 넣으면 9월 항목만 있는 배열이 저장되고, 검색어 변경 재시도는 `INDEX==='fail'`일 때만 실행된다.  
영향  실패한 달의 기사는 검색에서 조용히 빠지고, 같은 페이지에서 검색어를 바꾸어도 해당 달을 다시 가져오지 않는다.  
제안  부분 실패한 달을 기록해 검색 결과가 불완전함을 알리고, 다음 검색에서 그 달만 재시도한다.

## 정리

위치  CONFIG.md:145, CONFIG.md:167, news/summarizer.py:398  
무엇  설정 문서의 모델 폴백 순서와 회차 종료 조건이 현재 구현과 다르다.  
근거  문서는 마지막 모델을 `qwen3.6-27b`, 이동 방향을 아래 칸, 429 재시도 소진 뒤 동작을 회차 중단으로 설명한다; 실제 마지막 모델은 `gpt-oss-20b`, `chain_below()`는 현재 모델을 제외한 전체 체인, 618~631줄은 예비 모델로 같은 기사를 계속 처리한다.  
영향  운영자가 한도 소진 뒤 호출될 모델과 중단 시점을 잘못 판단하며, 문서의 사다리를 설정에 옮기면 기본 코드와 다른 체인을 사용한다.  
제안  실제 체인과 예비 모델까지 소진한 뒤 중단하는 조건을 문서에 반영한다.

위치  SPEC.md:34, SPEC.md:187, build.py:218  
무엇  현행 설계 제약으로 안내되는 SPEC이 실제 적용 중인 빌드 차단 필터를 금지한다.  
근거  SPEC은 빌드 단계의 유형 판정·키워드 컷 금지를 명시하지만 `build.py`는 `keyword_filter(raw, cfg.get("keywords", []), cfg.get("block_keywords"))`를 호출하며 `news/core/filters.py:130`은 제목 차단어가 걸린 기사를 제외한다; `README.md:88`은 SPEC을 설계 배경과 하지 말 것으로 연결한다.  
영향  유지보수 시 현재 필터를 유지해야 하는지 제거해야 하는지에 관해 코드와 명시된 작업 기준이 충돌한다.  
제안  당시 결정과 현재 유효한 제약을 구분하고 후속 차단 필터 도입 결정을 반영한다.

위치  scripts/graph.py:55, news/template.html:913, build.py:208, news/summarizer.py:94  
무엇  코드 연결이 없는 13개 중 6개는 기능이 유지되지만 슬러그 표시가 빠져 있다.  
근거  `facet-collapse-toggle`은 `news/template.html:913`의 접힘 저장·2091줄 토글, `fix-untranslated-titles`는 `news/summarizer.py:94`의 번역 지시, `split-main`은 `build.py:208`부터의 단계 함수, `thicken-why`는 `news/summarizer.py:96`의 2~3문장 지시와 `build.py:276`의 why_model 전달, `trim-ui-elements`는 `news/template.html:446`의 체크박스 표시·1295줄 정렬 선택, `unread-toggle-checkbox`는 `news/template.html:1441`의 체크박스로 남아 있다.  
영향  슬러그 기반 조회로 해당 구현을 찾지 못한다; 기능이 삭제된 것은 아니다.  
제안  현재 구현의 대표 위치에 기능 연결만 복원하고, 코드 없음 목록을 근거로 기능을 다시 만들지 않는다.

위치  scripts/graph.py:55, _workspace/done/bar-dropdown-cleanup/EXEC_PLAN.md:19, news/template.html:2058  
무엇  코드 연결이 없는 다른 6개는 후속 변경으로 처리 방식이나 최초 요구의 일부가 바뀌었으나 기능 기록만으로는 현재 상태가 드러나지 않는다.  
근거  `fix-hanja-leak`은 금지 지시(`news/summarizer.py:46`)를 유지하되 잔존 한자 미게시로 확장(`tests/test_summarizer_limit.py:117`); `fix-ime-composition-search`는 이벤트 가드에서 검색창을 보존하는 부분 갱신(`news/template.html:2058`)으로 대체; `restore-card-layout`은 썸네일 카드(`news/template.html:1335`) 유지·NEW/HOT 등 제거; `spec-revamp`는 월별 저장(`news/core/archive.py:91`)·검색(`news/template.html:1450`) 유지·초기 UI 대체; `tagbtn-below-sources`는 전용 줄 제거로 위치 요구 폐기; `tagbtn-restyle`은 강조색(`news/template.html:338`) 유지·버튼 위치는 검색 바(`news/template.html:1436`)로 변경됐다.  
영향  옛 완료 기록대로 기능을 복원하거나 테스트를 추가하면 현재 검색 방식·필터 배치와 충돌한다.  
제안  각 기록에 현재 구현과 후속 변경을 연결하고, 폐기된 위치·표시 요구는 다시 구현하지 않는다.

위치  scripts/graph.py:33, README.md:33  
무엇  나머지 `tidy-readme`는 문서 작업이어서 코드 연결 없음 목록에 들어간다.  
근거  README의 디렉터리·월별 샤드·워크플로 설명이 작업 결과이며, graph의 `EXTS`는 `.py`, `.html`, `.yaml`, `.yml`, `.sh`만 포함해 `.md` 문서를 탐색하지 않는다.  
영향  없음.  
제안  문서 작업으로 분류해 유지하고, 목록을 줄이려고 무관한 코드에 슬러그나 테스트를 추가하지 않는다.

위치  scripts/graph.py:55, tests/test_summarizer_limit.py:117, tests/test_tag_precision.py:84, tests/test_template.py:120  
무엇  테스트 연결이 없는 네 기능은 실제 관련 테스트가 있으며 슬러그 문자열만 연결되지 않았다.  
근거  `fix-hanja-residual`은 `test_persistent_hanja_not_published`, `remove-tag-cap`은 8개 태그 유지 검사, `move-pipeline-to-core`는 `tests/test_keyword_filter.py:15`·`tests/test_dead_links.py:16` 등의 core 직접 import와 기존 기능 검증, `unified-filter-panel`은 `tests/test_template.py:120`·133줄의 후속 바·패널 구성 검사로 확인된다.  
영향  테스트 부재로 해석하면 기존 회귀 검증을 중복 작성하거나, 후속 UI 변경으로 폐기된 초기 통합 구성을 다시 강제하게 된다; 현재 UI 검사는 문자열 검사이므로 실제 브라우저 동작 검증과는 구분해야 한다.  
제안  필요한 잔존 한자·태그 상한 회귀 검증은 기존 테스트를 유지하고, 순수 모듈 이동과 폐기된 초기 패널 구성을 위한 새 테스트는 만들지 않으며 기존 테스트와 기능의 연결을 보완한다.
