# dev-news 코드 점검 지시서

이 문서는 작업이 끝나면 삭제한다. 저장소에 남기지 않는다.

## 이 단계에서 할 일

**읽기만 한다. 코드를 고치지 않는다.** 문제 목록과 근거만 낸다.

고칠 항목은 사람이 목록을 보고 고른다. 그다음 단계에서 하나씩 고친다.

한 번에 다 고치면 무엇이 왜 바뀌었는지 아무도 모른다. 이 저장소는 실제로
운영 중이고, 하루 세 번 자동으로 기사를 수집해 공개 사이트에 배포한다.

---

## 프로젝트가 하는 일

개발·AI 뉴스를 하루 세 번(00시·08시·16시 KST) 수집해 한국어로 요약하고
정적 페이지로 발행한다. 사이트는 https://dev-news.net 이다.

```
수집 → 필터 → 중복 제거 → 선별 → LLM 요약·번역·분류 → 렌더 → 커밋·푸시
```

수집은 맥미니의 launchd 가 `scripts/publish.sh` 를 불러서 돈다. GitHub Actions
에서 옮겨왔다 — 러너 IP 가 뉴스 사이트에 차단됐고 cron 이 2~5시간씩 밀렸다.

## 규모

```
코드          8,442줄
  news/template.html   2,445   단일 페이지 앱 전체 (HTML+CSS+JS 한 파일)
  news/summarizer.py     672   LLM 호출·프롬프트·폴백 체인
  news/render.py         623   정적 HTML 생성
  build.py               362   파이프라인 조립
  news/core/*.py               필터·중복·점수·태그·아카이브

테스트        618개 · 46파일 · 전부 mock · 1초
기능          66개 (_workspace/ 폴더 기준)
```

## 이 저장소의 규약

**기능은 `_workspace/<슬러그>/` 폴더다.** 완료분은 `_workspace/done/` 아래에
있다. 코드 주석에 같은 슬러그를 `(add-source-silence-alert)` 꼴로 적어 어느
파일이 그 기능에 속하는지 표시한다.

`python scripts/graph.py` 를 돌리면 기능별 구성 파일과 끊긴 자리가 나온다.
현재 상태:

```
기능 66개 · 코드에 연결된 것 53개
코드 없음 13개    슬러그가 코드에 안 남은 기능
테스트 없음 4개    코드는 있는데 tests/ 에 흔적이 없다
```

주석은 한국어로 쓴다. **무엇을 하는지가 아니라 왜 그렇게 했는지**를 적는
관례다. 기존 주석을 읽어 보면 실측값과 사고 이력이 들어 있다.

## 절대 건드리지 말 것

| 경로 | 이유 |
|---|---|
| `docs/` | GitHub Pages 산출물. `scripts/rerender.sh` 가 생성한다. 직접 수정하면 다음 회차에 덮어쓰인다 |
| `docs/data/`, `data/` | 수집한 기사 원본. 2,300건 이상 누적돼 있다 |
| `.github/workflows/daily.yml`, `watchdog.yml` | 수동 수집과 중단 감시용 |

그리고 **대규모 일괄 변경을 하지 마라.** 맥미니가 하루 세 번 `git pull --rebase`
후 커밋·푸시한다. 같은 파일을 크게 바꾸면 충돌한다.

## 이미 아는 빚

아래는 발견된 것이고 고칠지는 아직 정하지 않았다. 다시 보고하지 않아도 된다.
**같은 종류가 더 있는지** 찾아 주면 된다.

**1. 쓰지 않는 LLM 출력을 매 회차 만든다.**
`config.yaml` 의 `relevance_gate: false` 라 `drop_irrelevant()` 가 안 돈다.
그런데 `news/summarizer.py` 의 `PROMPT` 는 매번 `분류` 항목을 요구한다. 지시문이
1,277자 중 239자(19%)를 차지하고 회차당 약 2,100토큰이 그렇게 나간다.

**2. 같은 결과를 두 모델이 만든다.**
`왜중요` 를 `gpt-oss-120b` 가 한 번 만들고 `qwen3.8-27b` 가 다시 만들어
덮어쓴다(`summarizer.py` 의 `base_why`). 폴백 장치라 낭비는 아니지만 문서에 없다.

**3. 주석이 선언한 전제가 현실과 어긋난 사례.**
`news/scrapers/rss.py` 에 "피드는 새 글이 없어도 기존 항목을 돌려주므로 0건은
곧 실패다" 라고 적혀 있었다. arXiv 는 주말에 항목 없는 껍데기를 준다. 매주
거짓 알람이 떴고 2026-09-20 에 고쳤다. **비슷하게 단정한 주석이 더 있는지 봐 달라.**

**4. 시간이 지나면 깨지는 테스트.**
`tests/test_seo.py` 의 한 건이 `batch` 날짜를 고정값으로 박아 두었고, 그 기능은
최근 3일 기사에만 적용된다. 작성 사흘 뒤부터 실패했는데 CI 가 없어 아무도 몰랐다.
**같은 방식으로 시한부인 테스트가 더 있는지 봐 달라.**

## 봐 주었으면 하는 것

우선순위 순이다.

**1. 조용한 실패**
에러 없이 잘못된 결과가 나가는 경로. 실제로 있었던 예 — LLM 호출이 429로
재시도까지 실패했는데 기사 한 건이 아무 신호 없이 게시되지 않았다.
`except: pass`, 빈 폴백, 검사 없는 기본값을 중심으로.

**2. 문서와 코드의 어긋남**
`SPEC.md`, `CONFIG.md`, 코드 주석이 현재 동작을 설명하는가. 특히 설정값이
"왜 그 값인지" 적힌 근거가 아직 유효한지.

**3. `news/template.html` 2,445줄**
HTML·CSS·JS 가 한 파일에 있다. 나누는 것이 실제로 이득인지, 아니면 단일 파일
배포라는 제약 때문에 그대로 두는 게 맞는지 판단해 달라. 이 파일은
`render.py` 가 읽어 치환하는 템플릿이다.

**4. 테스트 없는 4개 기능**
`fix-hanja-residual`, `move-pipeline-to-core`, `remove-tag-cap`,
`unified-filter-panel`. 테스트가 필요한지, 필요 없는 종류인지.

**5. 코드에 슬러그가 없는 13개 기능**
지운 기능인지, 작업하며 표시를 안 남긴 것인지 가려 달라.
`python scripts/graph.py --gaps` 로 목록이 나온다.

## 보고 형식

발견 하나에 다음 다섯 줄. 추측과 확인한 사실을 섞지 마라.

```
위치      파일:줄
무엇      한 문장
근거      코드 인용 또는 실행 결과. "그럴 것 같다" 는 근거가 아니다
영향      이것 때문에 무엇이 잘못되는가. 없으면 "없음"
제안      고친다면 어떻게. 고치지 말자는 제안도 답이다
```

심각도로 묶어 달라 — **동작이 틀림 / 위험 / 정리**.

"개선 여지가 있다", "고려해 볼 만하다" 같은 문장은 쓰지 마라. 무엇이 어떻게
잘못되는지 말할 수 없으면 그 항목은 빼라.

## 확인 방법

```bash
python -m pytest tests/ -q      # 618개, 1초
python scripts/graph.py         # 기능별 구성 파일
python scripts/graph.py --gaps  # 끊긴 자리
```

테스트를 고치거나 새로 쓰지 마라. 이 단계에서는 읽기만 한다.
