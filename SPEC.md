# dev-news 개편 스펙

이 문서는 dev-news 저장소의 개편 작업 지시서다. 작업 전에 `build.py`, `config.yaml`, `docs/index.html` 생성 로직, `data/` 구조를 먼저 읽고 실제 코드에 맞춰 적용할 것. 이 문서의 파일명·필드명은 예시이며, 기존 코드의 네이밍 컨벤션을 따른다.

---

## 0. 배경과 목표

dev-news는 매일 오전 9시(KST) GitHub Actions에서 개발·AI 뉴스를 수집해 정적 페이지(`docs/index.html`) 한 장을 만드는 개인용 도구다. 파이프라인: 수집 → 필터 → 점수화 → 중복제거 → 본문·썸네일 추출 → LLM 요약 → HTML 생성.

**현재 문제 3가지:**

1. **선별 실패** — 인기도 점수(업보트·댓글 기반)로 랭킹하니 "회사 대량해고", "AI 주가 상승", "AI 이대로 괜찮은가"류 기사가 상위를 차지한다. 사용자가 원하는 건 AI 활용 기술, 새 도구/모델, 실전 트렌드다. `source_base`/`source_weight` 수동 튜닝으로는 해결 불가 — 지표 자체를 교체해야 한다.
2. **데이터 상한** — 30일/300건에서 잘라내는 구조라 아카이브·검색·트렌드 분석이 불가능하다.
3. **UI 밀도** — 기사 카드 하나가 세로 약 280px을 차지해 한 화면에 2.5건만 보인다. 스캔용 피드가 아니라 대시보드처럼 생겼다.

**목표:** "300건을 훑어서 내가 원하는 20건만 남기는 큐레이터" + "무한 아카이브" + "고밀도 스캔 피드".

**불변 제약:**
- 서버·DB 없음. GitHub Actions + GitHub Pages + 저장소 내 JSON + localStorage로만 구성.
- `python build.py` 한 줄로 로컬에서도 동일하게 동작해야 함.
- LLM은 **Groq 전용** (무료 API라서 선택). GCP·Gemini·기타 유료 공급자 도입 금지. 멀티 공급자 추상화(`LLM_PROVIDER`)는 남겨도 되지만 새 공급자 지원을 추가하지 말 것.
- Groq 무료 한도(하루 1,000회 / 분당 요청·토큰 제한)에 도달할 수 있음을 전제로 설계할 것 — 1.6의 한도 대응이 필수 요구사항.
- 기존 기능(보관함, 읽음 표시, 마크다운 복사, `--demo`/`--no-ai` 모드)은 깨지지 않아야 함.

---

## Phase 1 — 태그 기반 선별 파이프라인 (최우선)

인기도 점수를 랭킹의 주 지표에서 제거하고, LLM 태깅으로 대체한다.

### 1.1 선별 방침 — 빌드 파이프라인에서는 차단하지 않는다

**결정: 이번 구현에서 빌드 파이프라인 수준의 차단 필터(LLM 유형 판정, 네거티브 키워드 컷)를 만들지 않는다.** 어떤 기사를 볼지/숨길지는 **클라이언트의 검색·필터 UI가 담당**한다 — 필터링을 데이터 생성 단계가 아니라 열람 단계로 미루는 설계. 데이터는 전부 보존되므로 기준이 바뀌어도 소급 적용이 가능하다.

- 선별은 현행대로 보조 점수(시간 감쇠, `per_source` 상한, `source_quota`) 기반 top_n 유지.
- 태그 어휘와 태그 기반 필터는 1B(데이터 2주~한 달 누적 후 코퍼스 분석으로 어휘 도출)에서 도입 — 그때 검색/필터 UI에 태그 칩이 붙으면서 "검색 필터 = 차단 필터"가 완성된다.
- **이번 구현에서 필수인 것은 1B의 재료 축적**: 수집 확대와 candidates 로그(1.4)는 그대로 진행한다. 후보를 넓게 기록하는 것과 페이지에 뭘 싣는가는 별개다.
- LLM은 이번 구현에서 **요약에만** 사용한다(1.3). 유형/태그 판정 호출 없음.

### 1.2 깔때기 구조

```
[1] 넓은 수집 (일 200~300건)
     ↓
[2] 중복 제거 + candidates 로그 기록 (전 후보, 네이티브 메타데이터 포함)
     ↓
[3] 보조 점수(시간 감쇠·per_source·quota)로 top_n(20) 선별 — 현행 로직 유지
     ↓
[4] 최종 top_n건만 본문 추출·썸네일·요약 (기존 비싼 처리, LLM은 여기서만)
```

- **[1] 수집 확대**: 소스별 fetch 수 상향. `config.yaml`의 `feeds`에 추가 권장 소스(사용자 확인 후): Hugging Face 블로그, arXiv cs.AI/cs.CL RSS, r/LocalLLaMA RSS, Simon Willison 블로그. 수집량은 `scraper.candidates_per_day` 류의 설정으로 조절 가능하게. 목적은 1B 어휘 도출용 코퍼스 축적 — 페이지 구성은 [3]의 상한들이 현행대로 통제한다.
- **[2] 기록**: 수집된 전 후보를 candidates 로그(1.4)에 기록. 차단·판정 없음.
- **[3] 선별**: 현행 점수 로직 그대로. 파이프라인 수준의 유형 판정·키워드 컷 없음(1.1).
- **[4] 요약**: 최종 선별분에만 본문 추출 + 요약. **요약 프롬프트와 재료를 교체한다** — 1.3 참조.

### 1.3 요약 프롬프트 교체 + 요약 재료 보강

현재 요약은 제목을 복창한다(예: 제목 "agent-skills — AI 코딩 에이전트를 위한 생산급 엔지니어링 기술" → 요약 "이 저장소는 AI 코딩 에이전트를 위한 생산급 엔지니어링 기술을 제공한다"). 원인은 프롬프트만이 아니라 **재료 부족** — GitHub 아이템은 trending 페이지의 한 줄 설명만 입력으로 쓰고 있어 LLM에게 제목 이상의 정보가 없다.

**재료 보강**: 최종 선별된 GitHub 아이템은 요약 전에 `https://raw.githubusercontent.com/<owner>/<repo>/HEAD/README.md`를 fetch(인증 불필요)해 앞부분 2,000~3,000자를 요약 입력에 포함한다. fetch 실패(404, 브랜치명 상이 등) 시 기존 설명만으로 진행하고 로그를 남긴다. 다른 소스는 기존 본문 추출 결과를 사용.

**프롬프트 요구사항**:
- 제목에 이미 있는 정보를 반복하지 말 것.
- 제목에 없는 새 정보 — 구체적으로 무엇이 새로운지, 왜 지금 주목받는지, GitHub 레포라면 스택·쓰임새 — 를 한 문장으로.
- 타깃 독자는 한 명으로 고정: "백엔드·AI를 다루는 개발자가 자기 일에 쓸지 판단할 수 있게".
- 덧붙일 정보가 없으면 빈 문자열을 반환할 것. 빈 요약인 기사는 UI에서 요약 줄을 렌더링하지 않는다.

### 1.4 판정 로그

[2]에서 수집한 **전체 후보**(선별 탈락분 포함)를 `data/candidates/` 아래 월별 파일에 기록: {url, 제목, 설명, 소스, **소스 네이티브 태그/토픽/언어/라이선스(원본 그대로)**, GitHub 아이템은 스타·포크 수 스냅샷, 선별 여부, 수집일}. 라이선스·토픽·언어는 GitHub API(`api.github.com/repos/<owner>/<repo>`)에서 LLM 없이 획득 — 비인증 시간당 60회 한도가 부족하면 무료 PAT를 Secrets에 추가(시간당 5,000회). 용도 두 가지: ① 1B단계에서 이 코퍼스를 분석해 태그 어휘를 도출, ② "선별된 기사 중 실제 보관/읽음 비율"로 판정 품질 측정. Phase 2의 월별 샤딩 규칙을 동일하게 적용.

### 1.5 점수 시스템 정리 + 스타 증가량(Δ)

- `기본점수 × 시간감쇠 × source_weight` 계산 로직은 동점 처리용으로 남기되, **UI에서 점수 표시를 제거**한다("226점" 등).
- **GitHub 아이템의 지표를 절대 스타 수에서 전일 대비 증가량(Δ)으로 교체한다.** 절대 스타는 "오래된 유명 레포"를, Δ는 "지금 뜨는 것"을 측정한다. 매일 커밋되는 샤드에 스타·포크 스냅샷이 남으므로 어제 값과의 차분으로 계산 — 추가 인프라 불필요. 어제 데이터에 없는 레포(첫 등장)는 trending 페이지의 "stars today" 값을 사용. Δ는 보조 점수 계산에 쓰고, UI 메타 줄에 `+540` 형태로 표시해도 좋다(의미 있는 숫자이므로 3.2의 점수 제거 대상에서 제외).
- `HOT` 뱃지(300점 기준)도 제거. `source_base` 등 점수 보정 설정은 하위 호환을 위해 읽되 문서에서 deprecated로 표기.

### 1.6 LLM 한도 대응 — 무한 재시도 금지 (필수)

**현재 버그**: 요약(번역) 도중 Groq 429(rate limit)를 만나면 60초 쿨타임 재시도가 무한 반복되어 빌드가 사실상 멈춘다. 요구사항: **한도에 걸리면 거기까지 처리된 결과로 페이지를 완성하고 정상 종료한다.**

모든 LLM 호출을 하나의 클라이언트 래퍼로 감싸고 다음 규칙을 적용:

- **재시도 상한**: 429 발생 시 `Retry-After` 헤더(없으면 60초)를 존중해 최대 2회만 재시도. 그래도 실패하면 `BudgetExhausted` 상태로 전환.
- **서킷 브레이커**: 한 번 `BudgetExhausted`가 되면 이번 실행의 **이후 모든 LLM 호출을 시도조차 하지 않고 건너뛴다**. (분당 한도든 일일 한도든, 반복 재시도는 시간 낭비이거나 영원히 실패한다.)
- **중요한 것 먼저 처리** — 한도가 꼬리를 자르도록: 요약(이번 구현에서 유일한 LLM 용도)은 최종 선별 랭킹 순서대로 수행한다. 한도에 걸리면 하위 기사부터 요약이 빠진다.
- **부분 실패의 처리**: 요약 중 한도 도달 → 요약 못 받은 기사는 **이번 회차에 게시하지 않는다**. seen.json에도 넣지 않으므로 다음 실행에서 다시 후보로 탐지된다. 그날 페이지는 요약까지 완료된 기사만으로 구성된다(20건 미만이어도 됨). 단, `window_hours`를 벗어나 다음 실행에서 재수집되지 않는 기사는 유실될 수 있음 — 이는 수용하는 트레이드오프로 한다.
- **seen.json 등록 시점**: 페이지에 실제로 실린 기사만 seen에 등록한다. 한도 때문에 처리 못 한 기사를 seen 처리하면 영영 소개 기회를 잃는다.
- **로그**: `[한도] 요약 14/20건 완료 후 한도 도달 — 나머지 6건은 이번 회차 미게시, 다음 실행에서 재탐지` 형태로 어디까지 처리됐는지 명시. 실행은 exit code 0으로 정상 종료 (Actions가 실패로 표시되지 않도록).
- **예산 상한 설정**: `config.yaml`에 `llm.max_calls_per_run` (기본 예: 50)을 두어 일일 한도를 하루 1회 실행이 다 소진하지 않도록 상한을 건다. 로컬 테스트 실행과 Actions 실행이 같은 날 겹쳐도 안전하게.

---

## Phase 2 — 데이터 아키텍처: 무한 누적

원칙: **저장과 표시를 분리한다.** 저장은 무제한 누적, 표시는 최근 30일.

### 2.1 월별 샤딩

```
data/articles/2026-08.json   # 이번 달 — 매일 이 파일만 수정됨
data/articles/2026-07.json   # 지난 달 — 이후 절대 수정하지 않음 (불변)
data/candidates/2026-08.json # 판정 로그도 동일 규칙
data/search-index.json       # 전체 색인 — 매일 재생성
data/seen.json               # 중복 방지 — 영구 유지로 변경
```

- 기존 `data/articles.json`은 마이그레이션 스크립트로 월별 샤드로 분리하고, 기존 파일은 제거한다. 마이그레이션은 멱등해야 한다(두 번 실행해도 안전).
- `max_items: 300` 상한과 `keep_days` 기반 삭제 로직을 **저장 계층에서 제거**한다. `keep_days`는 표시 계층(index.html에 굽는 범위)의 의미로만 남긴다.
- 이유(코드 주석에 남길 것): 단일 파일 무한 성장은 git 히스토리를 부풀리고 GitHub의 파일 100MB push 제한에 걸린다. 지난 달 샤드는 불변이라 git이 한 번만 저장하고, 브라우저 캐시 적중률도 100%가 된다.

### 2.2 검색 인덱스

`data/search-index.json`: **페이지에 실린 모든 기사**에 대해 {제목, url, 태그(1B 이후), 월(YYYY-MM), 소스, 수집일}만 담는다. 요약·본문 제외 — 기사당 100~200바이트 목표. 빌드마다 전체 재생성. 하루 20건 내외 × 수년치여도 수 MB 이내로 유지된다.

페이지에 실리지 못한 후보 전체(일 200~300건)는 인덱스에 넣지 않는다 — 넣으면 인덱스가 연 15MB+ 규모로 커져 즉시 검색이 무거워진다. 이들은 월별 `data/candidates/` 샤드에 남아 있으므로, 필요 시 아카이브 화면에서 "이 달의 전체 후보 보기"로 해당 월 candidates 샤드를 온디맨드 fetch해 조회하는 경로를 제공한다.

### 2.3 seen.json

30일 만료를 제거하고 영구 유지. URL 집합이라 용량 문제 없음. 무한 누적 시 "30일 지나 다시 트렌딩에 오른 기사가 중복 등장"하는 케이스를 막는 역할.

### 2.4 프론트엔드 데이터 로딩

- `index.html`에는 **최근 30일 기사만** 굽는다(현행 유지).
- 아카이브 조회: 검색창 사용 시 JS가 `search-index.json`을 fetch(1회, 이후 메모리 유지)해서 클라이언트에서 제목·태그 필터. 결과 클릭 시 해당 `월` 샤드를 fetch해서 요약 전문 표시. GitHub Pages가 `data/`를 정적 서빙하므로 추가 인프라 불필요. 경로는 상대 경로로(로컬 `file://` 열람과 Pages 서브패스 모두 대응 — fetch가 `file://`에서 실패하면 "로컬에서는 아카이브 검색 불가" 안내로 우아하게 강등).

### 2.5 북마크(보관함) 스냅샷

- localStorage `dev-news-saved`를 URL 배열에서 객체 배열로 변경: `{url, title, tags, month}`. 저장 시점에 스냅샷을 담는다.
- 보관함 목록은 이 스냅샷만으로 렌더링(네트워크 요청 0). 상세 열람 시에만 `month`로 해당 샤드 fetch.
- **마이그레이션**: 기존 URL-only 항목은 최초 로드 시 `search-index.json`에서 역추적해 객체로 승격. 인덱스에 없는 URL은 제목을 URL로 표시하며 유지.
- 읽음 표시(`dev-news-read`)는 현행 URL 집합 유지.

---

## Phase 3 — UI 개편: 대시보드 → 고밀도 피드

### 3.1 기사 행 (카드 → 리스트 행)

- 카드 레이아웃을 구분선(border-bottom 0.5~1px) 있는 행으로 교체. **기사당 세로 60~80px 목표** (현재 약 280px). 한 화면에 10건 이상 보여야 한다.
- 행 구성: `[상태 점] 제목 (1줄, 말줄임)` / `소스 · 시간 · 요약(있을 때만, 1줄)`.
- 제목: 14~15px, font-weight 600, 진한 색(#1a1a1a 급). 메타 줄: 12px, 회색.
- **읽음 표시(기존 기능 유지)**: 읽은 기사는 제목을 회색으로 흐림 처리 — 안 읽은 제목이 진해야 대비가 산다.
- 요약이 빈 문자열이면 요약 부분을 렌더링하지 않는다(행이 더 얇아짐).

### 3.2 제거 목록

- 기사별 점수 표시("226점") — 제거.
- 우측 OG 썸네일 — 제거 (축소되어 판독 불가, 노이즈).
- `NEW` 뱃지 — 6px 색점으로 교체. **읽음 처리된 기사에는 점도 표시하지 않는다** (새 글 신호와 읽음 신호가 공존하면 상쇄됨).
- `HOT` 뱃지 — 제거.
- 기사별 상시 체크박스 — 호버 시 노출(모바일에서는 상세 패널 안의 버튼)로 변경.
- 상단 통계 카드("누적 79건 · 회차 4개 · 소스 7곳...") — 카드 제거, 헤더 구석 한 줄 12px 텍스트로 강등.

### 3.3 필터 재구성

- **필터링은 클라이언트가 담당한다(1.1)**: 이번 구현에서는 검색창 + 소스별 칩 현행 유지. 태그 칩은 1B(어휘 확정 후)에서 도입 — 그때 태그 칩이 1차 필터가 되고 소스는 2차로 강등, 선택 조합은 localStorage에 저장해 재방문 시 기본 뷰로 적용되면서 "검색 필터 = 차단 필터"가 완성된다.
- 정렬(수집순/최신순/출처별)·기간·안읽음 토글은 유지하되 컨트롤 영역을 한 줄로 압축. 첫 기사가 뷰포트 상단에서 200px 이내에 시작하도록.
- 점수순 정렬은 제거(점수 자체가 강등되었으므로).

### 3.4 부수 기능 (여력 있으면)

- 상단 한 줄 트렌드: 이번 회차 태그 분포가 지난 7일 평균 대비 급증한 태그 표시 (예: "에이전트 관련 기사 이번 주 급증"). 데이터는 candidates 로그에서 계산.

---

## 작업 순서와 검수 기준

권장 순서 (각 단계가 독립적으로 배포 가능해야 함):

1. **Phase 2.1~2.3** (샤딩·인덱스·마이그레이션) — 데이터 기반부터. 태그 어휘 도출의 재료가 여기서 쌓이기 시작하므로 최우선. 검수: 마이그레이션 후 기존 기사가 월별 샤드에 보존되고, 빌드가 이번 달 샤드만 수정하며, 두 번 실행해도 안전.
2. **Phase 1** (수집 확대 + candidates 로그 + 요약 개선 + 한도 대응) — 검수: `--demo`와 `--no-ai` 모드 정상 동작. 로그에 "후보 N건 → 중복 제거 후 M건 → 최종 20건" 깔때기 수치 출력. candidates 로그에 전 후보와 소스 네이티브 메타데이터(태그·토픽·언어·라이선스·스타 스냅샷)가 원본 그대로 저장될 것. GitHub 아이템의 요약이 README 내용 기반으로 생성될 것(제목 복창 아님). **429를 강제로 발생시킨 테스트(mock)에서 무한 재시도 없이 부분 결과로 정상 종료(exit 0)하고, 미처리 기사가 seen에 등록되지 않을 것.**
3. **Phase 3** (UI) — 검수: 1440px 높이 뷰포트에서 기사 10건 이상 표시. 보관함·읽음·마크다운 복사 정상 동작. 라이트/다크 기존 지원 범위 유지.
4. **Phase 2.4~2.5** (아카이브 검색·북마크 스냅샷) — 검수: 30일 지난 기사를 검색으로 찾아 상세를 열 수 있고, 기존 북마크가 마이그레이션 후 제목과 함께 보일 것.
5. **1B (태그 도입)** — 이번 구현 범위 밖. 데이터 2주~한 달 누적 후 별도 세션에서: 코퍼스 분석 → 어휘 확정 → 소급 태깅 스크립트 → 태그 칩 UI(이것이 "검색 필터 = 차단 필터"의 완성). 어휘 도출 시 gitbap.com의 19개 카테고리(AI 에이전트, LLM 도구·RAG, MCP, 코딩 보조·CLI, 웹, 백엔드·DB, 데브옵스, 보안, 셀프호스팅, 데이터·크롤링 등)를 프라이어로 교차 검증(단, 레포 전용 분류라 뉴스 코퍼스에 1:1 적용 금지). 어휘 확정 후 GitHub Search API로 관심 토픽별 급상승 레포를 수집하는 트랙 추가를 검토(전역 trending 한 페이지의 커버리지 한계 보완).

## 하지 말 것

- 벡터 DB, 외부 DB, 백엔드 서버 도입 금지. 임베딩 기반 개인화는 이번 범위 밖(추후 candidates 로그 + 보관함 데이터로 검토 예정).
- GCP·Gemini 등 Groq 외 LLM 공급자 추가 금지.
- 429에 대한 무한/장시간 재시도 금지 — 재시도 2회 후 서킷 브레이커 (1.6).
- 빌드 파이프라인에 차단 필터(유형 판정·키워드 컷) 넣지 말 것 — 필터링은 클라이언트 몫 (1.1).
- 1B(태그 도입) 시: LLM 자유 태그 생성 금지(닫힌 어휘 강제), 기사별 개별 호출 금지(반드시 배치).
- 표시 계층에 30일 넘는 기사를 index.html에 굽지 말 것 (아카이브는 온디맨드 fetch).
- 기존 지난 달 샤드 파일 수정 금지.
- 새 프레임워크/빌드 도구 도입 금지 — 현행 파이썬 + 바닐라 JS 유지.
